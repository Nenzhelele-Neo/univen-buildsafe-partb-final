{
  "name": "univen-construction-system",
  "version": "1.0.0",
  "description": "UNIVEN construction management system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:schema": "node database/setup.js",
    "db:seed": "node database/seed.js",
    "db:setup": "npm run db:schema && npm run db:seed",
    "check": "node scripts/check.js",
    "test": "node --test test/*.test.js",
    "test:mysql": "node --test test/mysql.integration.js"
  },
  "engines": { "node": ">=22.0.0" },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^2.4.0",
    "mysql2": "^3.24.4"
  }
}
